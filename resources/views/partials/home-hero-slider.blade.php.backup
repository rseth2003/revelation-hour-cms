<section class="home-hero-slider" data-home-hero>
@include('partials.custom-hero-slides')
<div class="home-hero-track">
<article class="home-hero-slide is-active"><div class="home-hero-background home-hero-welcome"></div><div class="home-hero-shade"></div><div class="container home-hero-content"><p class="eyebrow">Word | Worth | Wonder</p><h1>Welcome to Revelation Hour Ministries International</h1><p class="home-hero-copy">Encounter God, grow through His Word, belong to a caring community and live with purpose.</p><div class="hero-actions"><a class="btn btn-primary" href="{{ route('visit') }}">Plan Your Visit</a><a class="btn btn-outline" href="{{ route('campuses') }}">Find a Campus</a></div></div></article>

<article class="home-hero-slide home-hero-media-slide">@if($featuredDailyWord?->poster_url)<div class="home-hero-background home-hero-blurred" style="background-image:url('{{ $featuredDailyWord->poster_url }}')"></div>@else<div class="home-hero-background home-hero-word"></div>@endif<div class="home-hero-shade"></div><div class="container home-hero-media-layout"><div class="home-hero-media-copy"><p class="eyebrow">Word of the Day</p><h2>{{ $featuredDailyWord?->title ?: 'Daily encouragement for your faith' }}</h2>@if($featuredDailyWord?->scripture_reference)<p class="home-hero-reference">{{ $featuredDailyWord->scripture_reference }}</p>@endif<p class="home-hero-copy">{{ $featuredDailyWord?->scripture_text ? \Illuminate\Support\Str::limit($featuredDailyWord->scripture_text,180) : ($featuredDailyWord?->message ? \Illuminate\Support\Str::limit($featuredDailyWord->message,180) : 'Read today’s Scripture and devotional message.') }}</p><a class="btn btn-primary" href="{{ route('daily-words') }}">Read Today’s Word</a></div>@if($featuredDailyWord?->poster_url)<div class="home-hero-poster-frame"><img src="{{ $featuredDailyWord->poster_url }}" alt="{{ $featuredDailyWord->title }}"></div>@endif</div></article>

<article class="home-hero-slide home-hero-media-slide">@if($featuredSermon?->thumbnail_url)<div class="home-hero-background home-hero-blurred" style="background-image:url('{{ $featuredSermon->thumbnail_url }}')"></div>@else<div class="home-hero-background home-hero-sermon"></div>@endif<div class="home-hero-shade"></div><div class="container home-hero-media-layout"><div class="home-hero-media-copy"><p class="eyebrow">Featured Sermon</p><h2>{{ $featuredSermon?->title ?: 'Grow through the Word of God' }}</h2>@if($featuredSermon?->speaker)<p class="home-hero-reference">Speaker: {{ $featuredSermon->speaker }}</p>@endif<p class="home-hero-copy">{{ $featuredSermon?->description ? \Illuminate\Support\Str::limit($featuredSermon->description,180) : 'Watch and listen to the latest sermons and ministry messages.' }}</p><div class="hero-actions">@if($featuredSermon?->youtube_url)<a class="btn btn-primary" href="{{ $featuredSermon->youtube_url }}" target="_blank">Watch Sermon</a>@endif<a class="btn btn-outline" href="{{ route('sermons') }}">Browse Sermons</a></div></div>@if($featuredSermon?->thumbnail_url)<div class="home-hero-poster-frame home-hero-sermon-frame"><img src="{{ $featuredSermon->thumbnail_url }}" alt="{{ $featuredSermon->title }}"></div>@endif</div></article>
</div>
<button class="home-hero-arrow home-hero-previous" type="button" data-home-hero-previous>‹</button><button class="home-hero-arrow home-hero-next" type="button" data-home-hero-next>›</button><div class="home-hero-dots" data-home-hero-dots></div><div class="home-hero-progress"><span data-home-hero-progress></span></div>
</section>
