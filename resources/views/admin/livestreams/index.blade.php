<x-admin-layout title="Livestreams | RHMI CMS" heading="Livestreams">
<div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h2 class="text-2xl font-bold text-[#072f68]">Livestream Management</h2><p class="text-sm text-slate-600">Schedule, publish and archive church broadcasts.</p></div><a href="{{ route('admin.livestreams.create') }}" class="rounded-xl bg-lime-500 px-5 py-3 font-bold text-[#072f68]">+ Add Livestream</a></div>
@if(session('success'))<div class="mb-5 rounded-xl bg-green-50 p-4 text-green-800">{{ session('success') }}</div>@endif
<div class="mb-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Total',$stats['total']],['Live Now',$stats['live']],['Upcoming',$stats['upcoming']],['Archived',$stats['ended']]] as [$label,$value])<div class="rounded-2xl border bg-white p-5"><p class="text-sm font-bold text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-black text-[#072f68]">{{ $value }}</p></div>@endforeach</div>
<div class="overflow-hidden rounded-2xl border bg-white"><table class="min-w-full divide-y"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Broadcast</th><th>Platform</th><th>Status</th><th>Schedule</th><th></th></tr></thead><tbody class="divide-y">@forelse($streams as $stream)<tr><td class="p-4"><div class="flex items-center gap-3">@if($stream->thumbnail_url)<img src="{{ $stream->thumbnail_url }}" class="h-16 w-24 rounded-lg object-cover">@endif<div><strong class="text-[#072f68]">{{ $stream->title }}</strong><p class="text-sm text-slate-500">{{ $stream->speaker }}</p></div></div></td><td class="p-4">{{ \App\Models\Livestream::PLATFORMS[$stream->platform] ?? ucfirst($stream->platform) }}</td><td class="p-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $stream->status==='live'?'bg-red-100 text-red-700':($stream->status==='upcoming'?'bg-blue-100 text-blue-700':($stream->status==='ended'?'bg-slate-200 text-slate-700':'bg-amber-100 text-amber-700')) }}">{{ strtoupper($stream->status) }}</span></td><td class="p-4 text-sm">{{ $stream->scheduled_start?->format('D, j M Y g:i A') ?: 'Not scheduled' }}</td><td class="p-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.livestreams.edit',$stream) }}" class="rounded-lg border px-3 py-2">Edit</a><form method="POST" action="{{ route('admin.livestreams.duplicate',$stream) }}">@csrf<button class="rounded-lg border px-3 py-2">Duplicate</button></form><form method="POST" action="{{ route('admin.livestreams.destroy',$stream) }}" onsubmit="return confirm('Delete this livestream?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-slate-500">No livestreams have been created.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $streams->links() }}</div>
</x-admin-layout>