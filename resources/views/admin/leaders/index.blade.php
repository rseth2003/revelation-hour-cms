<x-admin-layout title="Leadership" heading="Leadership"><div class="mb-6 flex justify-between"><div><h2 class="text-2xl font-bold text-[#072f68]">Leadership</h2><p class="text-sm text-slate-600">Manage public leadership profiles.</p></div><a href="{{route('admin.leaders.create')}}" class="rounded-xl bg-lime-500 px-5 py-3 font-bold text-[#072f68]">+ Add Leader</a></div>@if(session('success'))<div class="mb-5 rounded-xl bg-green-50 p-4 text-green-800">{{session('success')}}</div>@endif<div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">@forelse($items as $item)<article class="overflow-hidden rounded-2xl border bg-white">@if($item->photo_url)<img src="{{$item->photo_url}}" class="h-64 w-full object-cover">@endif<div class="p-5"><h3 class="font-bold text-[#072f68]">{{$item->name}}</h3><p>{{$item->role}}</p><div class="mt-4 flex gap-2"><a href="{{route('admin.leaders.edit',$item)}}" class="flex-1 rounded-lg border p-2 text-center">Edit</a><form class="flex-1" method="POST" action="{{route('admin.leaders.destroy',$item)}}" onsubmit="return confirm('Delete this leader?')">@csrf @method('DELETE')<button class="w-full rounded-lg border border-red-200 p-2 text-red-700">Delete</button></form></div></div></article>@empty<div class="col-span-full rounded-xl border border-dashed p-10 text-center">No leaders added.</div>@endforelse</div><div class="mt-5">{{$items->links()}}</div></x-admin-layout>
