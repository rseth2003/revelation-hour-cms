<x-admin-layout title="eLibrary | RHMI CMS" heading="eLibrary">
<div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h2 class="text-2xl font-bold text-[#072f68]">Library Resources</h2><p class="mt-1 text-sm text-slate-600">Manage books, study materials and public documents.</p></div><div class="flex gap-2"><a href="{{ route('admin.library-categories.index') }}" class="rounded-xl border bg-white px-5 py-3 font-bold text-blue-700">Categories</a><a href="{{ route('admin.library-resources.create') }}" class="rounded-xl bg-lime-500 px-5 py-3 font-bold text-[#072f68]">+ Add Resource</a></div></div>
@if(session('success'))<div class="mb-5 rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-green-800">{{ session('success') }}</div>@endif
<form method="GET" class="mb-6 flex flex-wrap gap-3 rounded-2xl border bg-white p-4"><input name="search" value="{{ request('search') }}" placeholder="Search resources" class="min-w-[220px] flex-1 rounded-xl border-slate-300"><select name="category" class="rounded-xl border-slate-300"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)request('category')===(string)$category->id)>{{ $category->name }}</option>@endforeach</select><button class="rounded-xl bg-[#072f68] px-5 py-2 text-white">Filter</button></form>
<div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">@forelse($resources as $resource)<article class="overflow-hidden rounded-2xl border bg-white shadow-sm">@if($resource->cover_url)<img src="{{ $resource->cover_url }}" class="h-64 w-full object-cover" alt="{{ $resource->title }}">@else<div class="grid h-64 place-items-center bg-gradient-to-br from-[#072f68] to-[#0d5fa8] text-5xl text-white">📖</div>@endif<div class="p-5"><div class="flex justify-between gap-3"><h3 class="font-bold text-[#072f68]">{{ $resource->title }}</h3><span class="text-xs font-bold">{{ $resource->is_published ? 'Published' : 'Draft' }}</span></div><p class="mt-1 text-sm text-slate-500">{{ $resource->author ?: 'RHMI' }} · {{ $resource->category?->name ?: 'Uncategorized' }}</p><div class="mt-3 flex gap-2 text-xs">@if($resource->is_featured)<span class="rounded-full bg-lime-100 px-2 py-1">Featured</span>@endif<span class="rounded-full bg-blue-50 px-2 py-1">{{ $resource->is_paid ? 'UGX '.number_format($resource->price_ugx) : 'Free' }}</span></div><div class="mt-5 flex gap-2"><a href="{{ route('admin.library-resources.edit',$resource) }}" class="flex-1 rounded-lg border px-4 py-2 text-center">Edit</a><form method="POST" action="{{ route('admin.library-resources.destroy',$resource) }}" class="flex-1" onsubmit="return confirm('Delete this resource?')">@csrf @method('DELETE')<button class="w-full rounded-lg border border-red-200 px-4 py-2 text-red-700">Delete</button></form></div></div></article>@empty<div class="col-span-full rounded-2xl border border-dashed bg-white p-12 text-center text-slate-500">No library resources have been added.</div>@endforelse</div><div class="mt-6">{{ $resources->links() }}</div>
</x-admin-layout>
