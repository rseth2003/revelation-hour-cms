<x-admin-layout title="FAQs" heading="Frequently Asked Questions"><div class="space-y-5"><div class="flex items-center justify-between"><p class="text-slate-500">Manage the questions shown on the homepage.</p><a href="{{ route('admin.faqs.create') }}" class="rounded-xl bg-[#1f2775] px-4 py-3 font-semibold text-white">Add FAQ</a></div><div class="space-y-3">@forelse($faqs as $faq)<article class="flex items-start justify-between gap-4 rounded-2xl border bg-white p-5"><div><h3 class="font-bold">{{ $faq->question }}</h3><p class="mt-1 text-slate-500">{{ \Illuminate\Support\Str::limit($faq->answer,140) }}</p><small>{{ $faq->is_published ? 'Published' : 'Hidden' }} · Order {{ $faq->sort_order }}</small></div><div class="flex gap-3"><a href="{{ route('admin.faqs.edit',$faq) }}" class="font-semibold text-blue-700">Edit</a><form method="POST" action="{{ route('admin.faqs.destroy',$faq) }}" onsubmit="return confirm('Delete this FAQ?')">@csrf @method('DELETE')<button class="text-red-700">Delete</button></form></div></article>@empty<div class="rounded-2xl border bg-white p-8 text-center">No FAQs added yet.</div>@endforelse</div></div></x-admin-layout>
