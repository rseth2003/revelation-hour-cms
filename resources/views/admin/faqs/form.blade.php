<x-admin-layout title="{{ $faq->exists ? 'Edit FAQ' : 'Add FAQ' }}" heading="{{ $faq->exists ? 'Edit FAQ' : 'Add FAQ' }}"><form method="POST" action="{{ $faq->exists ? route('admin.faqs.update',$faq) : route('admin.faqs.store') }}" class="mx-auto max-w-3xl space-y-5 rounded-2xl border bg-white p-6">@csrf @if($faq->exists) @method('PUT') @endif<label class="block font-semibold">Question<input name="question" value="{{ old('question',$faq->question) }}" required class="mt-2 w-full rounded-xl border p-3"></label><label class="block font-semibold">Answer<textarea name="answer" rows="7" required class="mt-2 w-full rounded-xl border p-3">{{ old('answer',$faq->answer) }}</textarea></label><label class="block font-semibold">Display order<input type="number" min="0" name="sort_order" value="{{ old('sort_order',$faq->sort_order ?? 0) }}" class="mt-2 w-full rounded-xl border p-3"></label><label class="flex items-center gap-2"><input type="hidden" name="is_published" value="0"><input type="checkbox" name="is_published" value="1" @checked(old('is_published',$faq->is_published ?? true))> Publish on homepage</label><div class="flex gap-3"><button class="rounded-xl bg-[#1f2775] px-5 py-3 font-semibold text-white">Save FAQ</button><a href="{{ route('admin.faqs.index') }}" class="rounded-xl border px-5 py-3">Cancel</a></div></form></x-admin-layout>
