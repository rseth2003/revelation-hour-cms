<x-admin-layout title="Praise Report Encouragements | RHMI CMS" heading="Encouragements"><div class="mb-6"><h2 class="text-2xl font-bold text-[#072f68]">Praise Report Encouragements</h2><p class="mt-1 text-sm text-slate-600">Approve only messages that are respectful and suitable for public display.</p></div>@if(session('success'))<div class="mb-5 rounded-xl bg-green-50 p-4 text-green-800">{{ session('success') }}</div>@endif<div class="space-y-4">@forelse($comments as $comment)<article class="rounded-2xl border bg-white p-5"><div class="flex flex-wrap justify-between gap-3"><div><strong>{{ $comment->name }}</strong><p class="text-sm text-slate-500">On: {{ $comment->praiseReport?->title }}</p></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">{{ ucfirst($comment->status) }}</span></div><p class="my-4">{{ $comment->message }}</p><div class="flex flex-wrap gap-2"><form method="POST" action="{{ route('admin.praise-reports.comments.approve',$comment) }}">@csrf @method('PATCH')<button class="rounded-lg bg-green-600 px-4 py-2 text-sm font-semibold text-white">Approve</button></form><form method="POST" action="{{ route('admin.praise-reports.comments.reject',$comment) }}">@csrf @method('PATCH')<button class="rounded-lg bg-slate-600 px-4 py-2 text-sm font-semibold text-white">Hide</button></form><form method="POST" action="{{ route('admin.praise-reports.comments.destroy',$comment) }}" onsubmit="return confirm('Delete this encouragement?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-4 py-2 text-sm font-semibold text-red-700">Delete</button></form></div></article>@empty<p>No encouragements have been submitted.</p>@endforelse</div><div class="mt-6">{{ $comments->links() }}</div></x-admin-layout>