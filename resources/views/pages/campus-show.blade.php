@extends('layouts.app')
@section('title',$campus->name.' | Revelation Hour Ministries International')
@section('content')
<section class="campus-detail-hero">@if($campus->cover_image_url)<div class="campus-detail-bg" style="background-image:url('{{ $campus->cover_image_url }}')"></div>@else<div class="campus-detail-bg campus-detail-fallback"></div>@endif<div class="campus-detail-shade"></div><div class="container campus-detail-hero-content"><p class="eyebrow">{{ $campus->is_main_campus?'Main Campus':'RHMI Campus' }}</p><h1>{{ $campus->name }}</h1><p>{{ $campus->address }}</p></div></section>
<section class="section"><div class="container campus-detail-grid"><div><p class="eyebrow">About This Campus</p><h2 class="section-title-left">Welcome to {{ $campus->name }}</h2>@if($campus->description)<div class="rich-copy">{!! nl2br(e($campus->description)) !!}</div>@endif<div class="campus-contact-box"><h3>Campus Information</h3><p><strong>Address:</strong> {{ $campus->address }}</p>@if($campus->district)<p><strong>District or City:</strong> {{ $campus->district }}</p>@endif @if($campus->phone_primary)<p><strong>Phone:</strong> <a href="tel:{{ $campus->phone_primary }}">{{ $campus->phone_primary }}</a></p>@endif @if($campus->phone_secondary)<p><strong>Alternative Phone:</strong> <a href="tel:{{ $campus->phone_secondary }}">{{ $campus->phone_secondary }}</a></p>@endif @if($campus->email)<p><strong>Email:</strong> <a href="mailto:{{ $campus->email }}">{{ $campus->email }}</a></p>@endif</div>@if($campus->map_url)<a class="btn btn-primary" href="{{ $campus->map_url }}" target="_blank">Open Directions</a>@endif</div><div>@if($campus->service_times)<div class="campus-services"><p class="eyebrow">Gather With Us</p><h2>Service Times</h2><div>{!! nl2br(e($campus->service_times)) !!}</div></div>@endif</div></div></section>
@if($campus->resident_pastor || $campus->pastor_image_url)<section class="section section-soft"><div class="container campus-pastor-grid"><div>@if($campus->pastor_image_url)<img src="{{ $campus->pastor_image_url }}" alt="{{ $campus->resident_pastor }}" class="campus-pastor-photo">@else<div class="campus-pastor-placeholder">Resident Pastor</div>@endif</div><div><p class="eyebrow">Campus Leadership</p><h2 class="section-title-left">{{ $campus->resident_pastor ?: 'Resident Pastor' }}</h2>@if($campus->pastor_bio)<div class="rich-copy">{!! nl2br(e($campus->pastor_bio)) !!}</div>@else<p>Pastor biography will be added through the CMS.</p>@endif</div></div></section>@endif
@endsection
